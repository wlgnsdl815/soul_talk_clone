import 'package:flutter/material.dart';

class MyCounselScreen extends StatelessWidget {
  const MyCounselScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'My Counsel',
      style: TextStyle(
        fontSize: 30,
      ),
    );
  }
}
